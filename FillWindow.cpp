#include "FillWindow.h"

/*void FillWindow::CreateWPlay(Builder& build)
{
		
	Buttom buttom(Color::Red, 200, 100, build.widthT - 250, build.heightT - 150);
	buttom.CreateTextButtom(30, "Informe");

	Buttom buttom1(Color::Blue, 200, 100, build.widthT - 250, build.heightT - 350);
	buttom1.CreateTextButtom(30, "Personaje");

	Buttom buttom2(Color::White, 200, 100, build.widthT - 250, 150);
	buttom2.CreateTextButtom(30, "DIA:\n " + to_string(build.contador));
}

void FillWindow::FillPlay(RenderWindow& window, Event event)
{
	if (event.type == Event::MouseButtonPressed) 
	{
		if (Mouse::getPosition(window).x > buttom.getPosX() &&
			Mouse::getPosition(window).y > buttom.getPosY() &&
			Mouse::getPosition(window).x < buttom.getPosX() + buttom.getWidthF() &&
			Mouse::getPosition(window).y < buttom.getPosY() + buttom.getHeightF())
		{
			texture.loadFromFile("Texture/Game0.jpg");
			IWInforme();
		}
	}
}
*/